services:
  - name: web
    type: web
    buildCommand: |
      cd client
      npm install
      npm run build
    startCommand: cd server && npm install && npm start
    envVars:
      - key: NODE_ENV
        value: production
